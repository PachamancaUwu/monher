@model IEnumerable<monher.Models.Producto>

<h2 class="text-center">Catálogo de Productos</h2>
<hr>

<div class="container">
    <div class="row">
        @foreach (var product in Model)
        {
            <div class="col-md-4 mb-4">
                <div class="card h-100">
                    <img class="card-img-top" src="@product.ImagenUrl" alt="@product.Nombre" style="height: 200px; object-fit: cover;">
                    <div class="card-body">
                        <h5 class="card-title">@product.Nombre</h5>
                        <p class="card-text">@product.Descripcion</p>
                        <p class="card-text"><strong>Precio: $@product.Precio</strong></p>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
